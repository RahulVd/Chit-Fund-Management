apiVersion: v1
kind: Service
metadata:
  name: chitfund-backend-service
spec:
  selector:
    app: chitfund-backend
  type: NodePort
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080
